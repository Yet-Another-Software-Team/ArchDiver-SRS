\chapter{Introduction}
\label{chap:introduction}

\section{Background}
\label{section:background}

In the current software development landscape, the industry has seen a surge in tools designed to accelerate the development lifecycle. However, a significant disparity remains between the tools available for low-level code implementation and those focused on the high-level detection and explanation of architectural integrity \cite{Lenhard2017}.
\\

Currently, the primary solutions for architectural analysis fall into two categories: Large Language Models (LLMs) and specialized static analysis platforms such as SonarQube \cite{SonarQube}. While these tools provide baseline insights, they present several limitations as effective detection systems for modern development teams:

\begin{itemize}
    \item \textbf{Operational Cost and Accessibility:} Enterprise-grade analysis tools often incur high licensing costs, making them inaccessible for smaller teams or individual researchers. Furthermore, cloud-dependent LLMs require continuous financial investment and a stable internet connection for consistent analysis.

    \item \textbf{Metric-Centric Detection:} Existing tools are highly effective at generating quantitative metrics (e.g., cyclomatic complexity) but often fail to detect \textit{structural} patterns. They typically report on localized code quality rather than identifying systemic architectural smells that emerge from the interactions between components.

    \item \textbf{The Interpretive Burden:} Most detection tools shift the entire responsibility of interpretation back to the developer. Without clear, natural language guidance, a developer must manually bridge the gap between a flagged numeric metric and the underlying architectural flaw it represents.

    \item \textbf{LLM Hallucinations and Bias:} Generic LLM-based solutions are prone to hallucinatory outputs when analyzing complex codebases. This can lead to the detection of non-existent issues or inappropriate suggestions that do not suit the specific structural constraints of the system.
\end{itemize}

\section{Problem Statement}
\label{section:problem-statement}

Modern systems are increasingly complex and are often developed at a rapid pace to meet market demands. This acceleration frequently comes at the cost of architectural integrity, leading to several critical challenges that \textbf{\usevar{\srsNickName}} aims to address:



\subsection{Neglect of High-Level Architectural Analysis}
\label{subsection:architectural-neglect}
While modern development workflows are saturated with automated testing and continuous integration, there is a notable lack of tooling dedicated specifically to the \textbf{architectural aspect} of software. Existing static analysis tools often focus on low-level code quality such as naming conventions, code style, and local design patterns, and therefore leave high-level architectural analysis to manual inspection or fragmented, ad-hoc tools. As a result, structural issues frequently remain undetected until they manifest as significant technical debt.

\subsection{Language Fragmentation and Tooling Silos}
\label{subsection:language-fragmentation}
The shift toward polyglot development and multi-language repositories has created a ``tooling silo'' effect. Most robust analysis solutions are language-specific, which creates significant friction when attempting to enforce architectural consistency across a diverse codebase. There is a distinct lack of cross-language frameworks capable of providing a unified architectural perspective regardless of the underlying programming languages used.

\subsection{The Explainability Gap}
\label{subsection:explainability-gap}
Even when advanced analysis is performed, a significant gap often exists between the complexity of the detection and the clarity of the resulting explanation. Existing tools frequently flag structural issues without providing the necessary context or natural language justification \cite{tantithamthavorn2020}. For a \textbf{software architect}, a simple ``warning'' is insufficient; without a metric-backed explanation of \textit{why} a particular design is problematic, it is difficult to reason about or prioritize improvements. While developers may understand the source code, they may lack the specialized knowledge to interpret raw metric values. \textbf{\usevar{\srsNickName}} aims to bridge this gap by providing clear, natural language explanations supported by quantitative metrics.

\section{Solution Overview}
\label{section:solution-overview}

\textbf{\usevar{\srsNickName}} aims to provide clear, natural language explanations for architectural smell detection. The system utilizes \textbf{Tree-sitter} to parse the structure of source code into a standardized \textbf{TOML-based Intermediate Representation (IR)}, which is then used as the foundation for smell detection via \textbf{GraphSAGE}. The resulting classification is used to generate natural language explanations for the detected smells, backed by quantitative metrics.

The \textbf{TOML-based IR} approach ensures the decoupling of the analysis model from the source code, allowing for easy adaptation to new languages and technologies.



\subsection{Features}
\label{subsection:features}

\begin{description}[style=multiline, leftmargin=3.5cm, font=\normalfont]
    \item[\textbf{Multi-Language Parsing}] Automated extraction of structural data from source code into a standardized \textbf{TOML-based Intermediate Representation (IR)}.
    \item[\textbf{Graph-Based Modeling}] Transformation of software components and their dependencies into a directed graph format optimized for \textbf{Graph Neural Network (GNN)} analysis.
    \item[\textbf{Architectural Smell Detection}] Algorithmic identification of structural \textit{anti-patterns}, including Cyclic Dependencies, God Components, and Hub-like dependencies.
    \item[\textbf{XAI Explanations}] Generation of concise, metric-backed natural-language explanations using \textbf{Explainable AI (XAI)} to justify why particular structures were classified as smells.
    \item[\textbf{Visualization}] A graphical interface for inspecting the system architecture, showing dependencies and flagged smells for manual review.
    \item[\textbf{MCP Server}] A Model Context Protocol server that allows agentic AI tools to interface with the analysis results. It provides a standardized way for AI agents to query detected smells, retrieve architectural justifications (XAI), and understand the codebase structure without direct manual extraction.
\end{description}

\section{Target User}
\label{section:target-user}

The design and functionality of \textbf{\usevar{\srsNickName}} are tailored towards the following individuals who are involved in the software architecture and design process:

\begin{description}[style=multiline, leftmargin=3.5cm, font=\normalfont]
    \item[\textbf{Software Architects:}] These users focus on the overall design and structure of software systems. They utilize the tool to detect architectural smells and use metrics in conjunction with explanations to aid with architectural decision making.
    \item[\textbf{Lead Developers and Tech Leads:}] These users are responsible for code reviews and ensuring the quality of software systems. They utilize the tool to understand the architecture and dependencies of the system via analysis results and graph visualization. They utilize the MCP server alongside agentic AIs for improved architectural refactoring capabilities.
    \item[\textbf{Software Engineering Researchers and Students:}] These users focus on the study and advancement of software engineering principles. They utilize the tool to analyze software systems for further study or extending research models.
    \item[\textbf{DevOps:}] These users focus on embedding automation and continuous integration into the software development process. They utilize the tool to monitor system dependencies and ensure that architectural changes do not break the system.
\end{description}

\section{Document Conventions}
\label{section:doc-conventions}

This document follows the formatting standards outlined below to ensure clarity and consistency across all sections.

\subsection{Typography}
To help the reader easily understand the contents of this document, the following styles are used:
\begin{description}[style=multiline, leftmargin=3cm, font=\normalfont]
    \item[\textbf{Bold Text:}] Used for key terms and primary project components (e.g., \textbf{Code Parser}).
    \item[\textit{Italic Text:}] Used for emphasis, document titles, or references to external papers and tools.
    \item[\texttt{Monospaced Font:}] Used for specific code snippets, file paths, or data formats (e.g., \texttt{config.toml}).
\end{description}

\section{Project Scope}
\label{section:project-scope}

The scope of \textbf{\usevar{\srsNickName}} is defined by the core features outlined in Section~\ref{subsection:features}.

\subsection{Out-of-Scope}
The following features are strictly excluded from the initial version of the application:
\begin{itemize}
    \item \textbf{Web-Based Interface:} The application is expected to utilize significant local resources (CPU/GPU) for graph analysis, making it unsuitable for a standard web-based deployment.
    \item \textbf{Version Control Integration:} The system will analyze only the current version of the provided source code; comparison between different versions in a VCS is not supported.
    \item \textbf{RESTful API Parsing:} The system will focus on deterministic connections between software components and will not attempt to parse or analyze RESTful API interactions, which are often dynamic and require runtime information.
\end{itemize}

\subsection{Assumptions and Dependencies}
\label{subsection:assumptions}

The core premise of \textbf{\usevar{\srsNickName}} language independence relies on the following technical assumptions and external dependencies:

\begin{itemize}
    \item \textbf{Tree-sitter Parser Availability:} It is assumed that Tree-sitter provides mature, accurate, and actively maintained grammar definitions for the target programming languages (e.g., Java, C\#, Go). The system's ability to abstract code into a unified Intermediate Representation (IR) is strictly dependent on the correctness of the Abstract Syntax Trees (ASTs) produced by these third-party parsers.
    \item \textbf{Syntactic Completeness:} We assume that the syntactic structures extracted by Tree-sitter are sufficient to infer high-level architectural relationships (such as dependencies and coupling) without requiring deep semantic compilation or a complete language server protocol (LSP) environment.
\end{itemize}

\section{Terminology}
\label{section:terminology}

\begin{description}[style=multiline, leftmargin=3cm, font=\normalfont, itemsep=1em]
    \item[\textbf{Architecture Smells}] Architectural issues that are not immediately obvious but can lead to long-term problems such as increased technical debt, reduced maintainability, or poor scalability. Specific smells include:
    \begin{itemize}
        \item \textbf{Cyclic Dependency:} A structural anti-pattern where two or more components depend on each other directly or indirectly, forming a closed loop in the dependency graph. This hinders modularity and complicates testing and reuse.
        \item \textbf{God Component:} A component that assumes an excessive amount of responsibility, often characterized by disproportionately high complexity and size metrics compared to the rest of the system.
        \item \textbf{Unstable Dependency:} A dependency relationship where a stable component depends on a less stable component, potentially leading to a ripple effect of changes.
        \item \textbf{Feature Concentration:} A smell where a single component or its immediate neighborhood realizes a disproportionately high number of system features.
        \item \textbf{Scattered Functionality:} An architectural flaw where related functionality or a single feature is dispersed across multiple components.
        \item \textbf{Dense Structure:} A characteristic of an architectural graph exhibiting a high degree of coupling with an excessive number of dependencies.
    \end{itemize}

    \item[\textbf{Language Independence}] The ability of the system to analyze source code written in multiple programming languages without requiring language-specific adjustments.

    \item[\textbf{Intermediate Representation (IR)}] A language-agnostic, TOML-based format. The system translates language-specific ASTs into this unified IR to perform standardized architectural analysis and visualization.

    \item[\textbf{Explainable AI (XAI)}] Techniques and methods that make AI models more transparent and interpretable to humans.

    \item[\textbf{Continuous Integration (CI)}] A software development practice where code changes are automatically built, tested, and integrated into the main codebase.

    \item[\textbf{GraphSAGE}] A graph neural network architecture that learns node representations by aggregating information from their neighborhood.

    \item[\textbf{Tree-sitter}] A parser generator that builds a concrete syntax tree for a given source code file, serving as the first step in the system's code extraction pipeline.

    \item[\textbf{Abstract Syntax Tree (AST)}] A tree representation of the source code's syntactic structure, generated via Tree-sitter, which strips away formatting details to focus on structural logic.

    \item[\textbf{MCP Server}] The program that exposes specific capabilities to AI applications through standardized protocol interfaces.
\end{description}
